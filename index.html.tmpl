<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ .Title }}</title>
    <style>
      html {
        font: 16px/1 sans-serif;
        height: 100%;
      }
      body {
        align-items: center;
        display: flex;
        margin: 0;
        min-height: 100%;
      }
      a {
        text-decoration: none;
      }
      a:not([href*="."]),
      a[href*="//"] {
        font-size: 0.875rem;
        font-weight: bold;
      }
      a:hover {
        color: #f00;
      }
      ul {
        box-sizing: border-box;
        flex: 1;
        font-size: 0.75rem;
        list-style: none;
        margin: 0;
        padding: 2rem;
        width: 100%;
      }
      li {
        border-top: 1px dotted #ddd;
        padding: 0.5rem 0.25rem;
      }
      li:last-child {
        border-bottom: 1px dotted #ddd;
      }
      li:hover {
        background: #f9f9f9;
      }
      dl {
        align-items: center;
        display: grid;
        margin: 0 auto;
        max-width: 50rem;
        width: 100%;
        grid: auto-flow / 1fr 1fr 1fr;
      }
      dt {
        font-size: 0.9375rem;
      }
      dd {
        font-family: "Menlo", "Andale Mono", monospace;
        margin: 0;
        text-align: right;
      }
      dt,
      dd {
        box-sizing: border-box;
        overflow: hidden;
        padding: 0 0.25rem;
        text-overflow: ellipsis;
        vertical-align: middle;
        white-space: nowrap;
        width: 100%;
      }
      dt:first-child {
        padding-left: 0;
      }
      dd:last-child {
        padding-right: 0;
      }
    </style>
  </head>
  <body>
    <ul>
      {{- range .List }}
        <li>
          <dl>
            <dt>
              <a href="{{.Name}}{{if .IsDir}}/{{end}}">{{.Name}}</a>
            </dt>
            <dd>{{.ModTime.Format "Jan. 02 '06 15:04:05"}}</dd>
            <dd>{{.Size}}</dd>
          </dl>
        </li>
      {{- else}}
        <li>&hellip;</li>
      {{- end}}
    </ul>
  </body>
</html>
